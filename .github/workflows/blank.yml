name: workflow_name
on: workflow_dispatch
jobs:
  job_name:
   runs-on: ubuntu-latest
   steps: 
     - name: step_name
       run: |
           echo "HELLO WORLD"
           echo "HELLO WORLD2"
           
