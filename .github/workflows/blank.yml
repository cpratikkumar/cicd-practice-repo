name: cicd
on: workflow_dispatch
jobs:
  build:
   runs-on: ubuntu-latest
   steps: 
     - name: building
       run: |
           echo "Build successfull"
   test:
    runs-on: ubantu-latest
    steps: 
       - name: testing
       run: echo "TESTING SUCCESSFUL"
   deploy:
    runs-on: ubantu-latest
    steps:
     - name: deploy
      run: "DEPLOY SUCCESSFUL"
           
          
